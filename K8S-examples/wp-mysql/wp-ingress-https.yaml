apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wp-ingress
spec:
  tls:
  - hosts:
    - karam.com
    secretName: wordpress-tls-cert
  rules:
  - host: karam.com
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: wordpress-svc
            port:
              number: 80

